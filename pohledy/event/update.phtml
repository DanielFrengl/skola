<h1>Update Event</h1>

<?php if (isset($this->data['event'])): ?>
    <form method="POST" action="/event/update/<?= $this->data['event']['id'] ?>">
        <label for="title">Event Title:</label>
        <input type="text" id="title" name="title" value="<?= htmlspecialchars($this->data['event']['title']) ?>" required>
        <br>

        <label for="start">Start Date:</label>
        <input type="datetime-local" id="start" name="start" value="<?= htmlspecialchars($this->data['event']['start_date']) ?>" required>
        <br>

        <label for="end">End Date:</label>
        <input type="datetime-local" id="end" name="end" value="<?= htmlspecialchars($this->data['event']['end_date']) ?>" required>
        <br>

        <button type="submit">Update Event</button>
    </form>
<?php else: ?>
    <p>Event not found.</p>
<?php endif; ?>
